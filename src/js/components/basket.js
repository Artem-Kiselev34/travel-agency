if (document.querySelector(".basket") !== null) {
  window.addEventListener("load", () => {
  if (document.querySelector(".hotel-card") !== null) {
    const count = document.querySelector('.account__count');
    count.classList.remove('is-hidden');
  }
  })
}
